---
title: "functionlities - load balancer"
draft: false
---

## Functionlities of Load Balancer

### Sticky Sessions - Cookie Names

- **Application based cookies** 
	 - Custom cookie : 
	     - Generated by the target
		 - Can include any custom attributes required by the application
		 - Cookie name must be specified individually for every target groups
		 - Don't use AWSALB , AWSALBAPP or AWSALGTG ( reserved for use for  ELB)
	- Application cookie : 
		- Generated by the load balancer
		- Cookie name is AWSALBAPP
-  **Duration based Cookie**
	-  Cookie generated by the load balancer		
	- Cookie name is AWSALB for ALB and AWSELB for CLB

### Cross Zone Load Balancing

-  With Cross zone lB Each load balancer instance distribute traffic evenly between all instances in all AZ

- If Cross zone load balancing is enabled : - 

   ![[Pasted image 20240802224247.png]] 

- If Cross zone load balancing is disabled : - 

   ![[Pasted image 20240802225223.png]]
   
- **Application load Balancer**
	- Enabled by default ( can disabled at the Target Group Level) 
	- No charges for inter AZ data
	
- **Network Load Balancer  & Gatewa Load Balancer** 
	- Disabled by default   
	- if enabled you pay charges ($) for inter AZ data
	
- **Classic Load Balancer**
	- Disabled by default   
	- No charges for inter AZ data if enabled 


### SSL / TLS - Basics

- An SSl certificate allows traffic between your client and load balancer to be encrypted in transit (in-flight encryption)
- SSL refers to Secure Socket Layer , used to encrypt connections
- TLS , Trasnfer Layer Security ,  which is newer version 
- Public SSL certificates are issued by Certificate Authorities (CA)
	-  Comodo , GoDaddy , GlobalSign etc
- SSL certificates have an expiration date (you set) and must be renewed
   ![[Pasted image 20240803000435.png]]
   
#### SSL - Server Name Indication (SNI)

-  SNI solves the problem of loading multiple SSL certificates onto one web server ( to serve multiple websites)
- It's  a 'newer' protocol , and requires tthe  client  to indicate the hostname of the target server in the initial SSL handlshake
-  The server will then find the correct certificate or return the default one

**important**
-  Only works for ALB , NLB (newer generation ) CloudFront
- Does not work CLB (older gen)

![[Pasted image 20240803145500.png]]

#### Elastic Load Balancer - SSL Certificates

- **Classic Load Balancer (v1)**
	-  Support only one SSL certificate 
	-  Must use multiple CLB for multiple hostname with multiple SSL certificates
	
- **Application Load Balancer (v2)** 
	- Supports multiple listeners with multiple SSL Certificates 
	- Use Server Name Indication (SNI) to make it work
	
- **Network Load Balancer (v2)** 
	- Supports multiple listeners with multiple SSL Certificates 
	- Uses SNI to make it work

### Connection Draining

- Feature naming
	- Connection Draining for CLB  
	- Deregistration- delay -  for  NLB & ALB

- Time to complete "in flight requests" while the instance is de-registering or unhealthy
- Stops sends new requests to EC2 instance while is de-registering
- Between 1 to 3600 sec (default : 300 sec)
- Can be disabled (value set 0)
- Set to low value if requests are low

----

[[aws]]
[[LB -  Load balancer]]

